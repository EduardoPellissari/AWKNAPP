services:
  - type: web
    name: awknapp
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: awknapp-db
          property: connectionString

databases:
  - name: awknapp-db
